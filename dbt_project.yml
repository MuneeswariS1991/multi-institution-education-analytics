name: 'education_analytics_dbt'
version: '1.0.0'
config-version: 2

profile: 'education_analytics_dbt'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  education_analytics_dbt:
    +materialized: table